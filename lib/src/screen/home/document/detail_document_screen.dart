import 'package:flutter/material.dart';

class DetailDocumentScreen extends StatelessWidget {
  const DetailDocumentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Dokumen kepelabuhan"),
      ),
      body: Container(),
    );
  }
}
